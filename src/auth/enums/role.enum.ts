export enum Worker {
  MANAGER = 'manager',
  CLEANER = 'cleaner',
  DRIVER = 'driver',
}

export enum Role {
  ADMIN = 'admin',
  SUPERMANAGER = 'supermanager',
  MANAGER = 'manager',
  CLEANER = 'cleaner',
  DRIVER = 'driver',
}